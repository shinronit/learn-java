import java.util.Scanner;  // Takes information that a user types  from a keyboard and stores it in a variable.

public class Tutorial9 {
    public static void main(String[] args) {
        Scanner cuckoo = new Scanner(System.in);

        int tuna = 5;
        int bass = 18;
        // ++ is used for quickly adding one to a variable.
        // increment operators
        // pre incrementing adding ++ sign before a variable.
        // post incrementing adding ++ sign after a variable.
        tuna += 9;
        System.out.println(tuna);

    }
}
